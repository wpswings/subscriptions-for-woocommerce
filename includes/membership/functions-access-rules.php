<?php
/**
 * Membership Layer — Access Rules CRUD + Resolver (Day 11 stub)
 *
 * Functions for storing, retrieving, and resolving content-access rules.
 * All rules live in the `wps_access_rules` option (no per-post meta).
 * Full implementation lands on Day 11 (June 18).
 *
 * @since      2.0.0
 * @package    Subscriptions_For_Woocommerce
 * @subpackage Subscriptions_For_Woocommerce/includes/membership
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

// Day 11: wps_get_access_rules(), wps_save_access_rules(), wps_add_access_rule(),
//         wps_delete_access_rule(), wps_rebuild_access_rules_index(),
//         wps_get_rules_for_object(), wps_object_is_restricted().
